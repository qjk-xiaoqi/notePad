const state = {
    isTray: false
};
const mutations = {
    getConfig() {
        // state.
    },
    saveTray(state, tray) {
        state.isTray = tray;
    }
}
const actions = {

}
const getters = {
}
export default {
    state,
    mutations,
    actions,
    getters
}